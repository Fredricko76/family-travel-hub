# Copy to .env (same folder). Values come from Supabase → Project Settings → API.
# The publishable/anon key is safe to ship in the app; row-level security does the protecting.
EXPO_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
EXPO_PUBLIC_SUPABASE_KEY=sb_publishable_...
